配置:
	name: git config --global user.name "citrus"
	email: git config --global user.email "384576183@qq.com"
	
使用git:
	git config --list 查看配置信息
	git status 查看当前地址是否是仓库
	
	untracked   added              modified  
	未跟踪---> 暂存---> 未修改---> 修改
	git add 添加到暂存
	git add * 将所有已修改(未跟踪)的 文件添加到暂存
	git commit -m 将暂存的文件存储到仓库
	git commit -a -m 提交所有未修改(未跟踪的文件不会提交)
	
	git restore <filename>重置已修改到未修改
	git rm <filename> 删除文件
	git restore --staged 取消暂存状态
	git mv from to 移动文件 重命名文件
	
	git branch 查看分支
	git branch <branch name> 创建分支
	可以创建多个分支, 分支和分支之间相互独立, 在分支上修改, 不会影响其他分支
	git branch -d <branch name> 删除分支
	git switch <branch name> 切换分支
	git switch -c <branch name> 创建并切换分支

	在开发中, 我们都是在自己的分支上编写代码, 代码编写完成后, 在将自己的分支代码合并到主分支中
	
	
	